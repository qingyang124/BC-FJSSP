# 训练脚本对比（仓库版 vs. 用户提供版）

本文概述当前仓库的 `src/train_imitation.py` 与用户提供脚本之间的主要差异。

## 超参数与命令行接口
- **仓库版**：额外暴露 `weight_decay`、学习率调度（`lr_factor`/`lr_patience`/`min_lr`）、验证间隔 `val_interval`、梯度裁剪 `clip_grad`、随机种子 `seed` 等开关，便于细致控制训练稳定性。
- **用户版**：命令行参数较少，权重衰减等超参数被写死在代码中，可调空间有限。

## 可复现性与稳定性
- **仓库版**：在 `start_train` 开头调用 `set_seed`，固定 Python/NumPy/PyTorch 随机性并启用确定性 CuDNN；每个 batch 进行梯度裁剪，默认打乱 mini-batch，降低梯度爆炸与方差。
- **用户版**：未设置随机种子，也不裁剪梯度，不同运行之间的差异和梯度波动更大。

## 训练循环与优化策略
- **仓库版**：集成 `ReduceLROnPlateau`，按验证指标自适应降学习率；按 `val_interval` 定期验证并追踪最优模型，如果验证未触发则回退保存最终权重。
- **用户版**：学习率固定，且仅在 episode>20 后才验证，容易错过早期过拟合信号，若验证未执行则可能不保存最佳模型。

## 数据生成与过滤
- **仓库版**：每个 episode 根据 `num_cases` 与 `number_group` 重采样专家数据，但不额外过滤动作数量（沿用环境已有逻辑）。
- **用户版**：同样重采样，但显式跳过可行动作数 ≤4 的样本，可能减少后期步骤数据量，降低多样性。

## 标签与指标处理
- **仓库版**：直接使用专家标签计算准确率，并记录每轮学习率以便调试。
- **用户版**：在训练时对每个作业的目标分布做归一化与 label smoothing（平滑系数 0.1），优化目标更平滑但与原精确标签有差异。

## 设备与缓存管理
- **仓库版**：默认使用 `cuda:0`（若可用）并在启动时清理显存缓存。
- **用户版**：使用通用 `cuda` 选择，不主动清理缓存，显存管理依赖默认策略。
